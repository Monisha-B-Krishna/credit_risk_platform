# I'm keeping the raw dataset out of git
data/*.csv

# Trained model files can be regenerated by running train.py, so no need
# to version them either.
models/*.pkl
models/*.joblib

# Python's own clutter
__pycache__/
*.pyc
.ipynb_checkpoints/

# My API keys live in .env
.env

# Virtual environment folder
venv/
.venv/
